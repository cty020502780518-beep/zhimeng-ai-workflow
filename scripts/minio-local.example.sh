#!/bin/bash

# 复制为 scripts/.minio-local.sh 后按你的本地 MinIO 启动规则修改

# 示例 1: 直接使用 MinIO 二进制
# export MINIO_START_CMD='minio server /absolute/path/to/minio-data --address :9000 --console-address :9001'

# 示例 2: 调用你自己的启动脚本
# export MINIO_START_CMD='/absolute/path/to/start-minio.sh'

